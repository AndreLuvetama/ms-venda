server:
  port: ${PORT:8081} #seta a porta 8081 se ela n�o for definida
spring:
  application:
    name: product-api
  # Details for our datasource
  datasource:
    url: jdbc:postgresql://localhost:5432/product-db
    username: postgres
    password: sa123
    initialize: true
    continue-on-error: true
    platform: postgresql
    maxActive: 3

    # Hibernate properties
  jpa:
    #show-sql: false
    hibernate:
      ddl-auto: update
      format_sql: true
    show_sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

